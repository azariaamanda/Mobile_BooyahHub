# TODO

## Splash screen fix
- [x] Identify that router initialLocation points directly to `/login`
- [x] Update `AppRouter` to use `/splash` as `initialLocation`
- [x] Add GoRoute for `/splash` that builds `SplashScreen()`
- [ ] Run app and verify splash shows then navigates to login after timer


