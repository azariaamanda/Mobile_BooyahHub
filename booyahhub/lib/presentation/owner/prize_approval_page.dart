import 'package:flutter/material.dart';

class PrizeApprovalPage extends StatelessWidget {
  const PrizeApprovalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Prize approval page')),
    );
  }
}

